{% extends 'base.html.twig' %}

{% block title %}{{ 'categories.delete.title'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'categories.delete.h1'|trans }}</h1>
    {{ form_start(form, { method: 'DELETE', action: url('category_delete', {'id': category.id}) }) }}
        {{ form_widget(form) }}
        <input type="submit" value="{{ 'action.delete'|trans }}" class="btn btn-primary" />
    {{ form_end(form) }}
{% endblock %}