{% extends 'base.html.twig' %}

{% block title %}password!{% endblock %}

{% block body %}
    <h1>{{ 'edit.user'|trans }}</h1>
    {{ form_start(form, { method: 'POST', action: url('app_password') }) }}
    {{ form_widget(form) }}
    <input type="submit" value="{{ 'action.save'|trans }}" class="btn btn-primary" />
    {{ form_end(form) }}
{% endblock %}
